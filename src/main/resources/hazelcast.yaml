hazelcast:
  cluster-name: hazelcast-example
  map:
    places:
      max-idle-seconds: 60
      eviction:
        eviction-policy: LRU
        max-size-policy: PER_NODE
        size: 300
  network:
    join:
      multicast:
        enabled: true
